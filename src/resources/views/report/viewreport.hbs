<div class="report-pvt-view">
  <h1 class="report-pvt-view__title">📄 Báo Cáo Tuần - {{report.reportName}}</h1>
  <p><strong>Số hiệu:</strong> {{report.number}}</p>
  <p><strong>Ngày tạo:</strong> {{formatDate report.createdAt}}</p>
  <p><strong>Người ký:</strong> {{report.signer}} ({{report.position}})</p>

  <hr />

  <!-- Nhóm 1: I. ĐÁNH GIÁ KẾT QUẢ TRIỂN KHAI -->
  <h2>I. ĐÁNH GIÁ KẾT QUẢ TRIỂN KHAI</h2>
  {{!-- Kết quả thực hiện BSC-KPI --}}
  <div class="report-pvt-view__section">
    <h3>1. Kết quả thực hiện BSC-KPI</h3>
    {{#each report.sections.bscKpi}}
      {{#if (eq this.type "text")}}
        <div class="report-subitem-view">
          <h4>{{this.title}}</h4>
          {{#each (splitLines this.content) }}
            <p class="report-line">- {{this}}</p>
          {{/each}}

        </div>
      {{/if}}
      {{#if (eq this.type "table")}}
        <div class="report-subtable-view">
  <h4>{{this.title}}</h4>
  <table>
    <thead>
      <tr>
        {{#each this.cells.[0]}}
          <th>{{this}}</th>
        {{/each}}
      </tr>
    </thead>
    <tbody>
      {{#each this.cells}}
        {{#if @index}}
          <tr>
            {{#each this}}
              <td>{{this}}</td>
            {{/each}}
          </tr>
        {{/if}}
      {{/each}}
    </tbody>
  </table>
</div>

      {{/if}}
    {{/each}}
  </div>

  

  {{!-- Công tác TUH --}}
  <div class="report-pvt-view__section">
    <h3>2. Công việc trọng tâm</h3>
    <h3>Công tác TUH</h3>
    {{#each report.sections.tuh}}
      {{#if (eq this.type "text")}}
        <div class="report-subitem-view">
          <h4>{{this.title}}</h4>
          {{#each (splitLines this.content) }}
  <p class="report-line">- {{this}}</p>
{{/each}}

        </div>
      {{/if}}
      {{#if (eq this.type "table")}}
        <div class="report-subtable-view">
  <h4>{{this.title}}</h4>
  <table>
    <thead>
      <tr>
        {{#each this.cells.[0]}}
          <th>{{this}}</th>
        {{/each}}
      </tr>
    </thead>
    <tbody>
      {{#each this.cells}}
        {{#if @index}}
          <tr>
            {{#each this}}
              <td>{{this}}</td>
            {{/each}}
          </tr>
        {{/if}}
      {{/each}}
    </tbody>
  </table>
</div>

      {{/if}}
    {{/each}}
  </div>

  {{!-- Công tác PTM --}}
  <div class="report-pvt-view__section">
    <h3>Công tác PTM</h3>
    {{#each report.sections.ptm}}
      {{#if (eq this.type "text")}}
        <div class="report-subitem-view">
          <h4>{{this.title}}</h4>
          {{#each (splitLines this.content) }}
  <p class="report-line">- {{this}}</p>
{{/each}}

        </div>
      {{/if}}
      {{#if (eq this.type "table")}}
        <div class="report-subtable-view">
  <h4>{{this.title}}</h4>
  <table>
    <thead>
      <tr>
        {{#each this.cells.[0]}}
          <th>{{this}}</th>
        {{/each}}
      </tr>
    </thead>
    <tbody>
      {{#each this.cells}}
        {{#if @index}}
          <tr>
            {{#each this}}
              <td>{{this}}</td>
            {{/each}}
          </tr>
        {{/if}}
      {{/each}}
    </tbody>
  </table>
</div>

      {{/if}}
    {{/each}}
  </div>

  {{!-- Công tác CNTT --}}
  <div class="report-pvt-view__section">
    <h3>Công tác CNTT</h3>
    {{#each report.sections.cntt}}
      {{#if (eq this.type "text")}}
        <div class="report-subitem-view">
          <h4>{{this.title}}</h4>
          {{#each (splitLines this.content) }}
  <p class="report-line">- {{this}}</p>
{{/each}}

        </div>
      {{/if}}
      {{#if (eq this.type "table")}}
        <div class="report-subtable-view">
  <h4>{{this.title}}</h4>
  <table>
    <thead>
      <tr>
        {{#each this.cells.[0]}}
          <th>{{this}}</th>
        {{/each}}
      </tr>
    </thead>
    <tbody>
      {{#each this.cells}}
        {{#if @index}}
          <tr>
            {{#each this}}
              <td>{{this}}</td>
            {{/each}}
          </tr>
        {{/if}}
      {{/each}}
    </tbody>
  </table>
</div>

      {{/if}}
    {{/each}}
  </div>

  {{!-- Công tác VHM --}}
  <div class="report-pvt-view__section">
    <h3>Công tác VHM</h3>
    {{#each report.sections.vhm}}
      {{#if (eq this.type "text")}}
        <div class="report-subitem-view">
          <h4>{{this.title}}</h4>
          {{#each (splitLines this.content) }}
  <p class="report-line">- {{this}}</p>
{{/each}}

        </div>
      {{/if}}
      {{#if (eq this.type "table")}}
        <div class="report-subtable-view">
  <h4>{{this.title}}</h4>
  <table>
    <thead>
      <tr>
        {{#each this.cells.[0]}}
          <th>{{this}}</th>
        {{/each}}
      </tr>
    </thead>
    <tbody>
      {{#each this.cells}}
        {{#if @index}}
          <tr>
            {{#each this}}
              <td>{{this}}</td>
            {{/each}}
          </tr>
        {{/if}}
      {{/each}}
    </tbody>
  </table>
</div>

      {{/if}}
    {{/each}}
  </div>

  {{!-- Công tác KHCP --}}
  <div class="report-pvt-view__section">
    <h3>Công tác KHCP</h3>
    {{#each report.sections.khcp}}
      {{#if (eq this.type "text")}}
        <div class="report-subitem-view">
          <h4>{{this.title}}</h4>
          {{#each (splitLines this.content) }}
  <p class="report-line">- {{this}}</p>
{{/each}}

        </div>
      {{/if}}
      {{#if (eq this.type "table")}}
        <div class="report-subtable-view">
  <h4>{{this.title}}</h4>
  <table>
    <thead>
      <tr>
        {{#each this.cells.[0]}}
          <th>{{this}}</th>
        {{/each}}
      </tr>
    </thead>
    <tbody>
      {{#each this.cells}}
        {{#if @index}}
          <tr>
            {{#each this}}
              <td>{{this}}</td>
            {{/each}}
          </tr>
        {{/if}}
      {{/each}}
    </tbody>
  </table>
</div>

      {{/if}}
    {{/each}}
  </div>

  <hr />

  <!-- Các nhóm còn lại bạn cũng làm tương tự, ví dụ Conclusion, Kiến nghị, Kế hoạch -->

  <h2>II. THỰC HIỆN KẾT LUẬN, CHỈ ĐẠO</h2>
  <div class="report-pvt-view__section">
    <h3>Nội dung kết luận, chỉ đạo</h3>
    {{#each report.sections.conclusion}}
      {{#if (eq this.type "text")}}
        <div class="report-subitem-view">
          <h4>{{this.title}}</h4>
          {{#each (splitLines this.content) }}
  <p class="report-line">- {{this}}</p>
{{/each}}

        </div>
      {{/if}}
      {{#if (eq this.type "table")}}
        <div class="report-subtable-view">
  <h4>{{this.title}}</h4>
  <table>
    <thead>
      <tr>
        {{#each this.cells.[0]}}
          <th>{{this}}</th>
        {{/each}}
      </tr>
    </thead>
    <tbody>
      {{#each this.cells}}
        {{#if @index}}
          <tr>
            {{#each this}}
              <td>{{this}}</td>
            {{/each}}
          </tr>
        {{/if}}
      {{/each}}
    </tbody>
  </table>
</div>

      {{/if}}
    {{/each}}
  </div>

  <hr />

  <h2>III. TỒN TẠI, KIẾN NGHỊ</h2>

  <div class="report-pvt-view__section">
    <h3>1. Kiến nghị với LĐTT/TCT</h3>
    {{#each report.sections.kientnghi_ldtt}}
      {{#if (eq this.type "text")}}
        <div class="report-subitem-view">
          <h4>{{this.title}}</h4>
          {{#each (splitLines this.content) }}
  <p class="report-line">- {{this}}</p>
{{/each}}

        </div>
      {{/if}}
      {{#if (eq this.type "table")}}
        <div class="report-subtable-view">
  <h4>{{this.title}}</h4>
  <table>
    <thead>
      <tr>
        {{#each this.cells.[0]}}
          <th>{{this}}</th>
        {{/each}}
      </tr>
    </thead>
    <tbody>
      {{#each this.cells}}
        {{#if @index}}
          <tr>
            {{#each this}}
              <td>{{this}}</td>
            {{/each}}
          </tr>
        {{/if}}
      {{/each}}
    </tbody>
  </table>
</div>

      {{/if}}
    {{/each}}
  </div>

  <div class="report-pvt-view__section">
    <h3>2. Kiến nghị với đơn vị khác</h3>
    {{#each report.sections.kientnghi_dvkhac}}
      {{#if (eq this.type "text")}}
        <div class="report-subitem-view">
          <h4>{{this.title}}</h4>
          {{#each (splitLines this.content) }}
  <p class="report-line">- {{this}}</p>
{{/each}}

        </div>
      {{/if}}
      {{#if (eq this.type "table")}}
        <div class="report-subtable-view">
  <h4>{{this.title}}</h4>
  <table>
    <thead>
      <tr>
        {{#each this.cells.[0]}}
          <th>{{this}}</th>
        {{/each}}
      </tr>
    </thead>
    <tbody>
      {{#each this.cells}}
        {{#if @index}}
          <tr>
            {{#each this}}
              <td>{{this}}</td>
            {{/each}}
          </tr>
        {{/if}}
      {{/each}}
    </tbody>
  </table>
</div>

      {{/if}}
    {{/each}}
  </div>

  <hr />

  <h2>IV. KẾ HOẠCH TUẦN TỚI</h2>

  {{!-- Kế hoạch TUH --}}
  <div class="report-pvt-view__section">
    <h3>Công tác TUH</h3>
    {{#each report.sections.kehoach_tuh}}
      {{#if (eq this.type "text")}}
        <div class="report-subitem-view">
          <h4>{{this.title}}</h4>
          {{#each (splitLines this.content) }}
  <p class="report-line">- {{this}}</p>
{{/each}}

        </div>
      {{/if}}
      {{#if (eq this.type "table")}}
        <div class="report-subtable-view">
  <h4>{{this.title}}</h4>
  <table>
    <thead>
      <tr>
        {{#each this.cells.[0]}}
          <th>{{this}}</th>
        {{/each}}
      </tr>
    </thead>
    <tbody>
      {{#each this.cells}}
        {{#if @index}}
          <tr>
            {{#each this}}
              <td>{{this}}</td>
            {{/each}}
          </tr>
        {{/if}}
      {{/each}}
    </tbody>
  </table>
</div>

      {{/if}}
    {{/each}}
  </div>

  {{!-- Kế hoạch PTM --}}
  <div class="report-pvt-view__section">
    <h3>Công tác PTM</h3>
    {{#each report.sections.kehoach_ptm}}
      {{#if (eq this.type "text")}}
        <div class="report-subitem-view">
          <h4>{{this.title}}</h4>
          {{#each (splitLines this.content) }}
  <p class="report-line">- {{this}}</p>
{{/each}}

        </div>
      {{/if}}
      {{#if (eq this.type "table")}}
        <div class="report-subtable-view">
  <h4>{{this.title}}</h4>
  <table>
    <thead>
      <tr>
        {{#each this.cells.[0]}}
          <th>{{this}}</th>
        {{/each}}
      </tr>
    </thead>
    <tbody>
      {{#each this.cells}}
        {{#if @index}}
          <tr>
            {{#each this}}
              <td>{{this}}</td>
            {{/each}}
          </tr>
        {{/if}}
      {{/each}}
    </tbody>
  </table>
</div>

      {{/if}}
    {{/each}}
  </div>

  {{!-- Kế hoạch CNTT --}}
  <div class="report-pvt-view__section">
    <h3>Công tác CNTT</h3>
    {{#each report.sections.kehoach_cntt}}
      {{#if (eq this.type "text")}}
        <div class="report-subitem-view">
          <h4>{{this.title}}</h4>
          {{#each (splitLines this.content) }}
  <p class="report-line">- {{this}}</p>
{{/each}}

        </div>
      {{/if}}
      {{#if (eq this.type "table")}}
        <div class="report-subtable-view">
  <h4>{{this.title}}</h4>
  <table>
    <thead>
      <tr>
        {{#each this.cells.[0]}}
          <th>{{this}}</th>
        {{/each}}
      </tr>
    </thead>
    <tbody>
      {{#each this.cells}}
        {{#if @index}}
          <tr>
            {{#each this}}
              <td>{{this}}</td>
            {{/each}}
          </tr>
        {{/if}}
      {{/each}}
    </tbody>
  </table>
</div>

      {{/if}}
    {{/each}}
  </div>

  {{!-- Kế hoạch VHM --}}
  <div class="report-pvt-view__section">
    <h3>Công tác VHM</h3>
    {{#each report.sections.kehoach_vhm}}
      {{#if (eq this.type "text")}}
        <div class="report-subitem-view">
          <h4>{{this.title}}</h4>
          {{#each (splitLines this.content) }}
  <p class="report-line">- {{this}}</p>
{{/each}}

        </div>
      {{/if}}
      {{#if (eq this.type "table")}}
        <div class="report-subtable-view">
  <h4>{{this.title}}</h4>
  <table>
    <thead>
      <tr>
        {{#each this.cells.[0]}}
          <th>{{this}}</th>
        {{/each}}
      </tr>
    </thead>
    <tbody>
      {{#each this.cells}}
        {{#if @index}}
          <tr>
            {{#each this}}
              <td>{{this}}</td>
            {{/each}}
          </tr>
        {{/if}}
      {{/each}}
    </tbody>
  </table>
</div>

      {{/if}}
    {{/each}}
  </div>

  {{!-- Kế hoạch KHCP --}}
  <div class="report-pvt-view__section">
    <h3>Công tác KHCP</h3>
    {{#each report.sections.kehoach_khcp}}
      {{#if (eq this.type "text")}}
        <div class="report-subitem-view">
          <h4>{{this.title}}</h4>
          {{#each (splitLines this.content) }}
  <p class="report-line">- {{this}}</p>
{{/each}}

        </div>
      {{/if}}
      {{#if (eq this.type "table")}}
        <div class="report-subtable-view">
  <h4>{{this.title}}</h4>
  <table>
    <thead>
      <tr>
        {{#each this.cells.[0]}}
          <th>{{this}}</th>
        {{/each}}
      </tr>
    </thead>
    <tbody>
      {{#each this.cells}}
        {{#if @index}}
          <tr>
            {{#each this}}
              <td>{{this}}</td>
            {{/each}}
          </tr>
        {{/if}}
      {{/each}}
    </tbody>
  </table>
</div>

      {{/if}}
    {{/each}}
  </div>

  <hr />

  <h2>NƠI NHẬN</h2>
  <pre class="report-pvt-view__receivers">{{report.receivers}}</pre>

  <hr />

  <h2>Thông tin ký báo cáo</h2>
  <p><strong>Người ký:</strong> {{report.signer}}</p>
  <p><strong>Chức vụ:</strong> {{report.position}}</p>

  <a href="/report/pvt/view" class="btn-back">⬅️ Quay lại danh sách</a>
</div>
